spring:
  application:
    name: springstudy
  datasource:
    url: jdbc:h2:mem:product
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /database-console